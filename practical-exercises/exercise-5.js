// Define an object type for a Car with properties brand (string) and year (number). 
// Create an instance and print its properties.

const car = { brand: string, year: number } = { brand: "Toyota", year: 2020 };
console.log(`Car Brand: ${car.brand}, Year: ${car.year}`);  